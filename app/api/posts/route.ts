import { NextRequest, NextResponse } from "next/server";

export default function handler(req: NextRequest, res: NextResponse) {
    // res.status(200).json({ message: 'Hello from the /api/posts endpoint!' });
  }